{
	"name": "@ssowhat/mock-server",
	"private": true,
	"version": "0.1.0",
	"description": "Deterministic mock-integration server for ssowhat.dev live transport mode — real SCIM responses from canned data. Throwaway credentials only; never a real IdP.",
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"dev": "tsx watch src/server.ts",
		"start": "tsx src/server.ts",
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@hono/node-server": "^2.0.11",
		"hono": "^4.12.31"
	},
	"devDependencies": {
		"@types/node": "^22.19.11",
		"tsx": "^4.23.1",
		"typescript": "^5.9.3",
		"vitest": "^4.1.10"
	}
}
